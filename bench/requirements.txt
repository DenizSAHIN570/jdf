# JDF vs PDF RAG benchmark — python rag_bench.py
# PDF parsers (each installed one becomes a pipeline; missing ones are skipped)
pymupdf>=1.24
pdfplumber>=0.11
pypdf>=4.0
# retrieval
numpy>=1.26
sentence-transformers>=3.0   # dense retriever; skip with --embedder none or use --embedder ollama:<model>
